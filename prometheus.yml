global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "pushgateway"
    # FIX: targets is correct, but ensure correct host:port
    static_configs:
      - targets: ["pushgateway:9091"] 

remote_write:
  # FIX: Correct target URL for the Mimir service inside the Docker network
  - url: "http://34.130.28.238:9009/api/v1/push" 
    headers:
      X-Scope-OrgID: "default"
